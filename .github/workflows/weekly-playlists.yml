# Triggers: schedule (below), and workflow_dispatch (Actions → Weekly playlist generation → Run workflow).
# Scheduled workflows run from the default branch; keep this file on main.
# Weekly slot: Sun 07:48 Europe/Amsterdam (minute 48 avoids :00 GitHub load).

name: Weekly playlist generation

on:
  schedule:
    # Sun 07:48 Europe/Amsterdam — avoids minute 0 (GitHub high-load note on :00).
    - cron: "48 7 * * 0"
      timezone: "Europe/Amsterdam"
  workflow_dispatch:

permissions:
  contents: read
  actions: write # setup-python pip cache

jobs:
  generate:
    runs-on: ubuntu-latest
    environment: PlaylistGenerator
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate playlists
        env:
          SPOTIPY_CLIENT_ID: ${{ secrets.SPOTIPY_CLIENT_ID }}
          SPOTIPY_CLIENT_SECRET: ${{ secrets.SPOTIPY_CLIENT_SECRET }}
          SPOTIPY_REDIRECT_URI: ${{ secrets.SPOTIPY_REDIRECT_URI }}
          SPOTIFY_REFRESH_TOKEN: ${{ secrets.SPOTIFY_REFRESH_TOKEN }}
          SOURCE_PLAYLIST_1: ${{ vars.SOURCE_PLAYLIST_1 }}
          TARGET_PLAYLIST_ID_1: ${{ vars.TARGET_PLAYLIST_ID_1 }}
          TARGET_PLAYLIST_ID_2: ${{ vars.TARGET_PLAYLIST_ID_2 }}
          TARGET_PLAYLIST_ID_3: ${{ vars.TARGET_PLAYLIST_ID_3 }}
          TARGET_PLAYLIST_ID_4: ${{ vars.TARGET_PLAYLIST_ID_4 }}
          TARGET_PLAYLIST_ID_5: ${{ vars.TARGET_PLAYLIST_ID_5 }}
        run: python generate.py
